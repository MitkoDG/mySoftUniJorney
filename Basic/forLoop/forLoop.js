function forLoop(input) {

    let name = input[0];
    let lastElement=name.length;

    for(let index = 0; index < lastElement; index++) {
        console.log(name[index]);
    }
    
   
} 
forLoop(['Dimitar']);