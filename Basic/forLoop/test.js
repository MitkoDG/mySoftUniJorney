function testSamples() {

    for (let i = 1; i <= 10; i++){
        console.log(i);
    }
    // let facturial = input[0];
    // let randomYear = Number(input[1]);

    // let result = 1;

    // for (let i = 1; i <= facturial; i++) {
    //     result = result * i;
        
    // }
    // console.log(facturial.length);
}

testSamples(["This message has exactly eleven words. One more as it's allowed!"]);