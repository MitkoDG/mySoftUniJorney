function characterSequence(input) {

        let wordN = input[0];
        let wordLength = wordN.length;

    for (let i = 0; i < wordLength; i++) {
        console.log(wordN[i]);
    }
}
characterSequence(["ice cream"]);